{
	// Place your snippets for python here. Each snippet is defined under a snippet name and has a prefix, body and 
	// description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the 
	// same ids are connected.
	// Example:
	// "Print to console": {
	// 	"prefix": "log",
	// 	"body": [
	// 		"console.log('$1');",
	// 		"$2"
	// 	],
	// 	"description": "Log output to console"
	// }
	"main": {
		"prefix": "main",
		"body": [
			"if __name__ == '__main__':",
			"    ${1:pass}"
		],
		"description": "main"
	},
	"import numpy as np": {
		"prefix": "import numpy as np",
		"body": [
			"import numpy as np",
		],
		"description": "import numpy as np"
	},
	"with open(filepath, 'rb') as f": {
		"prefix": "with open(filepath, 'rb') as f",
		"body": [
			"with open(filepath, 'rb') as f",
			"    f",
		],
		"description": "with open(filepath, 'rb') as f"
	},
	"datetime.now().strftime('%Y%m%d_%H%M%S')": {
		"prefix": "datetime.now().strftime('%Y%m%d_%H%M%S')",
		"body": [
			"datetime.now().strftime('%Y%m%d_%H%M%S')",
		],
		"description": "datetime.now().strftime('%Y%m%d_%H%M%S')"
	},
	"glob": {
		"prefix": "glob(dir_name+os.sep+'*')",
		"body": [
			"glob(dir_name+os.sep+'*')",
		],
		"description": "glob"
	},
	"glob recursive": {
		"prefix": "glob(dir_name+os.sep+'*', recursive=True)",
		"body": [
			"glob(dir_name+os.sep+'*', recursive=True)",
		],
		"description": "glob recursive"
	},
}